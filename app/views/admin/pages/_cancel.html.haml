- if extracted?(model.parent)
  :javascript
    function redirect_to_archive_index() {
      $$('#content .buttons a').first().writeAttribute('href', "#{url_for index_page_for_model_with_extract}")
    }

    document.observe("dom:loaded", redirect_to_archive_index)
